{
  "name": "@poc/sample-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp dev --host 127.0.0.1 --port 3211",
    "build": "vp build",
    "preview": "vp preview --host 127.0.0.1 --port 4211",
    "check": "rm -rf dist && vp check",
    "test": "vp test run"
  },
  "devDependencies": {
    "@poc/tsconfig": "workspace:*",
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3",
    "vite-plus": "0.1.18"
  }
}
