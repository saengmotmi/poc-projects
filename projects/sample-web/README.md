# sample-web

Vite+ 기준으로 가장 얇게 검증할 수 있는 웹 POC 샘플입니다.

## 명령

```bash
corepack yarn workspace @poc/sample-web dev
corepack yarn workspace @poc/sample-web build
corepack yarn workspace @poc/sample-web check
corepack yarn workspace @poc/sample-web test
```
